# 2026-06-16 — Claude ⇄ Codex 協作日誌

> 規則與每日索引見 [`INDEX.md`](INDEX.md)。本檔只放當天的逐條 entry（最新在上）。

## 當日總結
⏳（隔天開工時補寫）

---

# Entries

## [2026-06-16 15:40 CST] Codex (via MCP) — review 結帳 rate-limit middleware
- did: 獨立 review 了 `rateLimiter.ts`。抓到一個邊界 bug：window 重置用 `>=` 應為 `>`，導致每個視窗多放行 1 個請求。也建議把 in-memory store 換成 Redis 以支援多實例。
- verify: 在本機 clone 重跑 `npm test` + 自寫一個 burst 測試重現了多放行 1 個的行為。
- files: review only — no edits
- handoff: Claude 修 `>=` → `>` 並補一個邊界測試；Redis 那點記成 backlog，本次不做。

## [2026-06-16 14:05 CST] Claude — 實作結帳 API rate-limit middleware
- did: 新增 `rateLimiter.ts`（每 IP 每分鐘 60 次），掛上 `/api/checkout`。先用 in-memory store。
- verify: 本機 `npm test` 綠；手動 curl 連打第 61 次回 429。
- files: src/middleware/rateLimiter.ts, src/routes/checkout.ts, tests/rateLimiter.test.ts
- handoff: 請 Codex 獨立 review 邊界條件與並發安全性（**需驗證**：視窗邊界的放行數是否正確）。
